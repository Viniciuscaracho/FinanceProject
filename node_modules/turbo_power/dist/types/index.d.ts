import * as Turbo from "@hotwired/turbo";
import { TurboStreamAction, TurboStreamActions } from "@hotwired/turbo";
import { BrowserAdapter } from "@hotwired/turbo/dist/types/core/native/browser_adapter";
import * as Actions from "./actions";
import * as Utils from "./utils";
export * as Actions from "./actions";
export * as Utils from "./utils";
declare global {
    interface Window {
        Turbo: typeof Turbo & {
            navigator: {
                adapter: BrowserAdapter;
            };
        };
    }
}
export declare function initialize(streamActions: TurboStreamActions): void;
export declare function register(name: string, action: TurboStreamAction, streamActions: TurboStreamActions): void;
declare const _default: {
    initialize: typeof initialize;
    register: typeof register;
    Actions: typeof Actions;
    Utils: typeof Utils;
};
export default _default;
