version: '3.2'
services:
  procfy-mail:
    image: 'sj26/mailcatcher'
    ports:
      - "1080:1080"
      - "1025:1025"

  procfy-redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  procfy-db:
    image: postgres:14-alpine
    environment:
      - POSTGRES_PASSWORD=postgres
    ports:
      - "9999:5432"
    volumes:
      - "procfy-db-data:/var/lib/postgresql/data"

volumes:
  procfy-db-data: