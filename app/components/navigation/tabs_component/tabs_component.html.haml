%div.flex.flex-col.space-y-4.sm:space-y-6{ data: { controller: :tabs, tabs_active_tab_class: 'bg-primary-500 text-white hover:bg-primary-500 hover:text-white' }}
  - if header.present?
    = header
  - if body.present?
    = body
  = content
