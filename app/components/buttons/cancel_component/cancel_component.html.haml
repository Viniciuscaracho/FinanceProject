= link_to @value, @url, { id: @options.fetch(:id, "cancel"),
                          disabled: @options.fetch(:disabled, false),
                          class: "btn#{@options.fetch(:class, '')}",
                          data: @options.fetch(:data, { turbo_frame: '_top' }) }
